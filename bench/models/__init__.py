"""Bench-vendored model definitions (ResNet-18, char-LM, NanoGPT).

All models are minimal PyTorch implementations — no torchvision.models,
no transformers, no huggingface. They're carried verbatim inside the
bench so the repo stays self-contained.
"""
